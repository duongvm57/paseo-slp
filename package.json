{
  "name": "paseo-slp",
  "version": "0.0.0-candidate",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "install:slp": "bash install.sh",
    "test": "node --test tests/*.test.mjs",
    "e2e": "node e2e/cli.mjs",
    "check": "node bin/slp.mjs identity"
  }
}
